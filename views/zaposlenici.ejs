<!DOCTYPE html>
<html>

<head>
    <title>
        <%= title %>
    </title>
    <link rel='stylesheet' href='styleTablica.css' />
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" integrity="sha384-MCw98/SFnGE8fJT3GXwEOngsV7Zt27NXFoaoApmYm81iuXoPkFOJwJ8ERdknLPMO" crossorigin="anonymous">
    <script type="text/javascript">
        function dodaj() {
        var x = document.getElementById("korisnikdodaj");
        if (x.style.display === "none") {
             x.style.display = "flex";
        } else {
        x.style.display = "none";
        }
    }
    </script>
</head>

<body>
    <h1><%= title %></h1>
    <button onclick="dodaj()" class="btn btn-primary">Novi Račun</button>
    <div style="display:none;width:100%;" id="korisnikdodaj">
    <form name="dodajform" action="noviracun" method="post" accept-charset="utf-8">
        <h4>Novi račun</h4>
        <div class="row">
            <label>datum： </label>
            <input type="text" name="datum" placeholder="Datum">
        </div>
        <div class="row">
            <label>Način plaćanja：</label>
            <input type="text" name="nacinplacanja" placeholder="Način plaćanja">
        </div>
        <div class="row">
            <label>ID zaposlenika：</label>
            <input type="text" name="idzaposlenik" placeholder="ID zaposlenika">
        </div>
        <div class="row">
            <input class="btn" type='submit' value='Submit'>       
        </div>
    </form>
    </div>
    <table class="table">
    <thead class="thead-dark">
        <tr>
            <% for ( var i = 0 ; i < fields.length ; i++){ %>
            <th><%= fields[i].name %></th>
            <% } %>
        </tr>
    </thead>
            <% for ( var i = 0 ; i < data.length ; i++){ %>
            <tr>
                <td>
                    <%= data[i].id  %>
                </td>
                <td>
                    <%= data[i].datum %>
                </td>
                <td>
                    <%= data[i].nacin_placanja_id  %>
                </td>
                <td>
                    <%= data[i].id_zaposlenik  %>
                </td>
            </tr>
            <% } %>
    </table>
</body>

</html>
